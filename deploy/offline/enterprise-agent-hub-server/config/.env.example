# Copy to config/.env before running scripts/install.sh.
POSTGRES_DB=enterprise_agent_hub
POSTGRES_USER=enterprise_agent
POSTGRES_PASSWORD=change-me-before-install
SERVER_PORT=8080
POSTGRES_IMAGE=postgres:16-alpine
SERVER_IMAGE=enterprise-agent-hub-server:offline
