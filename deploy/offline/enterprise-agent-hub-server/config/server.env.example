# Copy to config/server.env and adjust for the offline environment.
SPRING_PROFILES_ACTIVE=prod
SERVER_PORT=8080
# Optional: align with the mounted storage volume used by docker-compose.yml.
ENTERPRISE_AGENT_STORAGE_ROOT=/var/lib/enterprise-agent-hub/storage
# Optional: configure package/update retention paths in application config if needed.
